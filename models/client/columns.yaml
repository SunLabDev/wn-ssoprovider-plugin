# ===================================
#  List Column Definitions
# ===================================

columns:
    id:
        label: ID
        searchable: true

    host:
        label: sunlab.ssoprovider::lang.fields.host

    callback_url:
        label: sunlab.ssoprovider::lang.fields.callback_url
